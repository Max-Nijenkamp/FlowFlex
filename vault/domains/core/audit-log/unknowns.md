---
domain: core
module: audit-log
type: unknown
build-status: planned
status: wip
color: "#4ADE80"
updated: 2026-06-20
---

# Audit Log — Unknowns / UNVERIFIED

Parent: [[_module]]

## `*(assumed)*` markers carried from spec

- The PII denylist is driven by a per-model `$auditExclude` list *(assumed)* — the exact mechanism (per-model property vs. central config) is unconfirmed.
- IP address is captured inside `properties` *(assumed)* rather than a dedicated column.

> [!warning] UNVERIFIED — needs confirmation: the folder slugs for the foundation panel/tenancy dependency modules used in [[_module]] links.
